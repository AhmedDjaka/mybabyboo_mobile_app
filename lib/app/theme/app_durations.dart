class AppDurations {
  static const Duration fast = Duration(milliseconds: 300);
  static const Duration normal = Duration(milliseconds: 500);
  static const Duration slow = Duration(milliseconds: 800);
  static const Duration verySlow = Duration(milliseconds: 1000);

  // Scènes du Splash narratif — durées contemplatives définitives
  static const Duration splashScene1 = Duration(milliseconds: 3000);
  static const Duration splashScene2 = Duration(milliseconds: 3000);
  static const Duration splashScene3 = Duration(milliseconds: 3200);
  static const Duration splashScene4 = Duration(milliseconds: 3200);
  static const Duration splashScene5 = Duration(milliseconds: 3500);
  static const Duration splashLogoReveal = Duration(milliseconds: 1800);
}
